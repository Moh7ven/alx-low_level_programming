#include "main.h"

/**
  * _isupper - check if the letter is upper
  * @x: the number to be checked
  * 
  * Return: 1for upper letter 0 for anny else
  */
int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (i)
	}
	
	return (0);
